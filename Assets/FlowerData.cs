using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(menuName = "FlowerData")]
public class FlowerData : ScriptableObject
{
    // Start is called before the first frame update
    public int id;
    public int sugarPoints;
}
